<%= form_for(@hold_deal) do |f| %>
  <% if @hold_deal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hold_deal.errors.count, "error") %> prohibited this hold_deal from being saved:</h2>

      <ul>
      <% @hold_deal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
